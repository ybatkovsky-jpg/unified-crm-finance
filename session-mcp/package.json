{
  "name": "session-context-mcp",
  "version": "2.0.0",
  "description": "Universal MCP server for session context management — auto-discovers project context files, stores session scratchpad, works with any project",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "session-context-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
