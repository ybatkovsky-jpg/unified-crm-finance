# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Организация монорепозитория | Feature-sliced структура внутри apps/web | Модули пересекаются по данным — нужна гибкость между автономией и совместным использованием кода | Yes | agent |
| D002 |  | infrastructure | Docker Compose стратегия | Всё в Docker с volume mounting | Environment parity как в проде HMR работает через volumes одна команда запуска для всех окружений | Yes | agent |
| D003 |  | deployment | Deploy стратегия | Coolify для autodeploy на VPS | Автодеплой из Git UI для мониторинга SSL из коробки сэкономит время при разработке от 0 до продакшена | Yes | agent |
| D004 |  | architecture | Tech stack | Next.js 16 React 19 Python 3.12 FastAPI PostgreSQL 16 Prisma 6 RabbitMQ | По спецификации docs tech stack md промышленные стеки с хорошей экосистемой | Yes | agent |
| D005 |  | architecture | Error handling стратегия | Прагматичный подход для M001 критичные сценарии без over engineering | M001 инфраструктурный milestone критично чтобы сервисы не крашились и ошибки были видны в логах | Yes | agent |
| D006 | M001 - Infrastructure and Data Model | architecture | Hybrid backend architecture (Next.js API Routes + Python FastAPI) | Next.js API Routes for primary CRUD/auth/UI API; Python FastAPI for background processing (AI, email, Celery tasks) | Minimizes rewrite (40K+ lines of AI code preserved), provides type-safe end-to-end TypeScript for user-facing operations, leverages Python's superior AI/data ecosystem for intensive processing. Single PostgreSQL database shared between Prisma (read/write authority) and SQLAlchemy (read-mostly views). | false | agent |
| D007 | M001/S05 — FastAPI worker infrastructure | architecture | Async driver choice for FastAPI worker PostgreSQL connectivity | asyncpg driver with SQLAlchemy create_async_engine() | asyncpg is async-native, non-blocking, and ~20% faster than psycopg2 for async workloads. It integrates with SQLAlchemy 2.0 async engine, matching FastAPI's async model. Sync drivers (psycopg2) would block the event loop. | false | agent |
| D008 | M001/S05 — FastAPI worker infrastructure | architecture | RabbitMQ consumer library for FastAPI worker | aio-pika with connect_robust() for RabbitMQ consumer | aio-pika is async-native (matches FastAPI), has built-in connect_robust() for production resilience (auto-reconnect + state recovery), and provides direct consumer lifecycle control. Celery is pre-installed from S01 but can be added later for complex retry patterns — both can coexist. | false | agent |
| D009 | M001/S06 — CI/CD pipeline planning | ci-cd | CI/CD tool and strategy for M001 infrastructure | GitHub Actions with two workflows: ci.yml (quality gates) and deploy.yml (Docker build + Coolify webhook) | GitHub Actions is native to GitHub (project hosting), provides 2000 free minutes/month, and integrates seamlessly with GitHub Container Registry. Splitting CI and deploy workflows allows quality gates on all branches while deployment only happens on main. Uses ruff for Python linting (10-100x faster than flake8/black/isort) and pytest for async FastAPI testing. Coolify GitHub Marketplace action triggers autodeploy to VPS 64.188.56.25. | false | agent |
| D010 | M001/S06 — CI/CD pipeline slice | architecture | CI/CD tool and pipeline structure for M001 infrastructure | GitHub Actions with two workflows: ci.yml (quality gates: ruff lint + pytest test) and deploy.yml (Docker build + GHCR push + Coolify webhook) | GitHub Actions is native to GitHub (project hosting), provides 2000 free minutes/month, and integrates seamlessly with GitHub Container Registry. Splitting CI and deploy workflows allows quality gates on all branches without deploying, while deploy only runs on main. Ruff replaces flake8/black/isort (10-100x faster, single tool). GHCR avoids Docker Hub rate limits and is pre-authenticated via GITHUB_TOKEN. | false — GitHub Actions is industry standard for GitHub-hosted projects; migration cost to other platforms (CircleCI, GitLab CI) is high with low benefit | agent |
| D011 | S06 CI/CD implementation | architecture | CI/CD pipeline platform and tools | GitHub Actions with ruff for linting and pytest for testing, Docker build to GHCR, Coolify webhook for deployment | GitHub Actions provides native CI/CD integration with GitHub repository. ruff is modern fast Python linter replacing flake8/black/isort. pytest is standard Python test framework with async support. GHCR integrates seamlessly with GitHub repos. Coolify provides simple autodeploy to VPS. | yes | agent |
| D012 | Planning M002 after M001 completion, when apps/web is empty and Prisma schema exists but migrations not applied. | M002 CRM module | Milestone M002 slice decomposition strategy | 4 vertical slices: S01 Contact CRUD API (high risk), S02 Contact List UI, S03 Interactions API & UI, S04 Contact Detail & Integration. S01 fully decomposed into 4 tasks (migrations, Prisma wrapper, API routes, client helpers). Progressive planning: S02-S04 as sketches to be refined after S01 completion. | S01 carries highest risk (Prisma migrations + API routes foundation). Proving this works first validates the data layer before UI. S02-S04 are lower risk UI/integration work that depends on working API. Progressive planning allows adjustment based on S01 findings (e.g., if Prisma schema issues discovered). | false | agent |
| D013 | Planning M002 S02 — apps/web has no CSS, no component library, no PostCSS config. Need to choose the right versions compatible with Next.js 16.2.9 + Turbopack. | frontend | UI stack for M002 CRM frontend (S02 Contact List UI) | Tailwind CSS v4 with @tailwindcss/postcss plugin, shadcn/ui latest (canary with Tailwind v4 support), lucide-react for icons. No tailwind.config.js — configuration is CSS-based via @theme blocks in globals.css. PostCSS plugin replaces the standalone tailwindcss CLI. | Next.js 16.2.9 ships with Turbopack and targets Tailwind CSS v4 which uses CSS-based configuration instead of tailwind.config.js. shadcn/ui latest supports Tailwind v4 natively. This is the standard stack for new Next.js 16 projects and eliminates the config format mismatch risk. | false | agent |
| D014 |  | architecture | How to handle required authorId on Interaction in MVP without auth middleware | Accept authorId as a required POST body parameter with validation, rather than schema migration to make it optional or seeding a hardcoded system user | Keeps schema unchanged (avoids migration risk), works with seeded users (admin@local) for tests, and is trivially replaceable with real auth middleware (req.user.id) later. For tests, create a test User first then use its ID. For UI, pass authorId from parent context or lookup seeded user. | Yes | agent |
| D015 |  | architecture | Extract shared API client helpers to dedicated file | Created src/lib/api/shared.ts with ApiClientError, parseApiError, and parseJson; refactored contacts.ts to import from shared instead of duplicating | Avoids copy-paste duplication between ContactApiClient and InteractionApiClient. The task allowed either copying or extracting; extracting keeps the codebase DRY and makes it easier to add future API clients | Yes | agent |
| D016 |  | architecture | DealHistory tracking via separate table for audit trail of stage transitions | Separate DealHistory table with fromStageId, toStageId, comment, changedBy, changedAt fields. Each moveStage call creates one DealHistory record before updating the deal's stage. | Enables full audit trail of how a deal moved through the pipeline. Queryable and filterable unlike JSON-field approach. Simpler than full event sourcing. | Yes | agent |
| D017 |  | architecture | Contract versioning via separate ContractVersion table with incremental version numbers | Separate ContractVersion table. addVersion queries latest version number and increments by 1. Each version stores content, changes description, and timestamp. | Allows seeing what changed between contract versions. Supports rollback if needed. Simpler than per-contract tables. | Yes | agent |
| D018 |  | architecture | Deal to Contract conversion with bidirectional link (deal.contractId ↔ contract.dealId) | convertFromDeal method in ContractRepository creates Contract from Deal data and sets both deal.contractId and contract.dealId. Throws if contract already exists for deal. | Deal and contract are lifecycle-linked. Bidirectional link enables navigation in both directions without extra queries. 1:1 relationship doesn't need a junction table. | Yes | agent |
| D019 |  | architecture | Soft-delete pattern (deletedAt timestamp) for Deal and Contract | Use deletedAt nullable timestamp instead of physical row deletion. Repositories filter where deletedAt IS NULL by default; softDelete sets deletedAt = now(). | Allows recovery of mistakenly deleted deals/contracts. Preserves referential integrity. Statistics don't break after deletion. Consistent with M002 contacts pattern. | Yes | agent |
| D020 |  | architecture | Production как отдельная сущность внутри Project | Создать новые модели Production и ProductionStage в Prisma схеме | Производство (плиты, столешницы) имеет собственные этапы иタイムлайн, независимые от основных этапов проекта. Может быть несколько производств на один проект. Отдельная сущность позволяет independently track статус каждого производства. | Yes | collaborative |
| D021 | M004 planning | architecture | Production как отдельная сущность | Production — это отдельная model в Prisma схеме (1:N к Project) | Производство имеет собственный pipeline (этапы до поставки на объект), может быть несколько на один проект, и отличается от основных этапов проекта. Типы: PLATE (плитные материалы), COUNTERTOP (столешницы из кварца/акрила). | Yes | agent |
| D022 | M004 planning | architecture | ProjectMember с мульти-ролями | Один пользователь может иметь несколько ролей в одном проекте через множественные ProjectMember записи | В одной команде пользователь может быть одновременно и менеджером, и технологом, и исполнителем. Это business requirement, а не ошибка данных. | Yes | agent |
| D023 | M004 planning | library | Gantt реализация с vis-timeline | Использовать библиотеку vis-timeline для MVP Gantt | vis-timeline проверена, работает без тяжелых зависимостей, покрывает MVP requirement. Critical path, resource leveling, export PDF — отложены на M007. | Yes | agent |
| D024 | M004 planning | integration | File attachments через FileEntity | Использовать существующую FileEntity model с MinIO storage | FileEntity уже существует в схеме. Не нужно новой инфраструктуры. Просто нужна UI интеграция. | Yes | agent |
| D025 | M004 planning | architecture | Cascade close логика | Акт выполненных работ может закрыть либо проект, либо сделку. Другая сущность закрывается автоматически (cascade). | Гибкость для разных business процессов. Иногда акт = завершение проекта (сделка follows), иногда акт = завершение сделки (проект follows). | Yes | agent |
| D026 | M004 S02 planning | M004/S02 | Project externalNumber format and generation | externalNumber is client-provided (no auto-generation in API) | Project uses externalNumber as a unique string field without @default in schema. Unlike Deal.number (auto-generated С-YYYY-NNNNN), Project numbering may follow external systems (contract numbers, client references). API accepts externalNumber from client; validation enforces uniqueness via @unique constraint. Auto-generation can be added later if needed. | true | agent |
| D027 | M004 S02 planning | M004/S02 | ProjectStage and ProjectMember API exposure in S02 | ProjectStage/ProjectMember CRUD included in S02 ProjectRepository | S02 goal is full Project CRUD layer. ProjectStage and ProjectMember are child entities with onDelete: Cascade to Project. Repository needs methods to manage stages (createStage, updateStage) and members (addMember, removeMember, findMembers) for complete project management. API routes focus on Project root; nested endpoints for stages/members deferred to S04 detail page work. | true | agent |
| D028 | S06 implementation - no explicit type column in Production model | M004/S06 | Production type storage | Store production type (PLATE/COUNTERTOP) in attributes JSON field | Production schema has no explicit type field. Using attributes.type JSON field provides flexibility for additional production-specific metadata without schema changes. This follows existing pattern where other models use attributes for extensible properties. | true | agent |
| D029 | M004 S07 planning - File attachments required for drawings, acts, and specs | M004/S07 | File attachment pattern for Deal and Project | Add nullable foreign keys (drawingFileId, actFileId for Deal; specFileId for Project) following Contract.signedFileId pattern | FileEntity model already exists with MinIO storage. Adding direct foreign key relations follows the existing schema pattern (Contract.signedFileId, BOM.sourceFileId) and keeps the model simple. Many-to-many junction tables would be overkill for this MVP use case. | true | agent |
| D030 | M004 S07 planning - Cascade close requirement for акт-signed projects | M004/S07 | Cascade close direction and implementation | Project completion triggers Deal closure (unidirectional cascade from Project to Deal) via Prisma transaction | Business requirement: акты (acceptance acts) close the project, which then closes the associated deal. Unidirectional cascade (Project → Deal) is simpler than bidirectional sync and matches the real-world workflow where projects are the execution layer and deals are the commercial layer. | true | agent |
| D031 |  | architecture | Procurement module route prefix | UI routes at /procurement/*, API routes at /api/counterparties | Follows established pattern from CRM module (/crm/* for UI, /api/contacts for API). Procurement is a distinct top-level domain separate from CRM, warranting its own route prefix. API routes use the resource name (counterparties) not the module name, matching /api/contacts convention. | Yes | agent |
| D032 | Planning M005/S02 BOM slice, 2026-06-23. Worker at apps/worker/app/main.py is a skeleton with no AI endpoints. | architecture | S02 BOM Excel parsing: client-side SheetJS vs Python worker AI pipeline | Client-side Excel parsing with SheetJS (xlsx npm package). Defer Python worker AI parsing pipeline to a later enhancement. | The Python worker currently has only health check endpoint, RabbitMQ consumer, and database connection — no webhook endpoints, no Excel processing logic, no LLM integration, no callback mechanism. Building the full AI pipeline would require 4+ new worker components. Client-side SheetJS covers 80% of the value (structured Excel→BOM items with header-based column mapping) with zero infrastructure changes. The data model and API are unchanged regardless of parsing source, so the AI enhancement can be added later without breaking changes. | yes — when Python worker gains webhook/LLM capabilities | agent |
| D033 | Planning M005/S02, discovered project page uses Card layout not tabs | architecture | S02 BOM UI integration: Card section vs tabbed interface on project detail page | Add BOM as a Card section in the existing sequential Card layout on apps/web/src/app/projects/[id]/page.tsx, alongside Production, Files, and Related sections. Do NOT introduce a tabbed interface. | The existing project detail page uses a stacked Card layout with sections: Details, Stages/Gantt, Production, Files, Related, Team, Metadata. Adding a BOM Card following this exact pattern keeps the UI consistent and avoids introducing a new UI pattern (tabs) that would require migrating existing sections. The research suggested tabs but was written before verifying the actual page.tsx code structure. | yes — if the project page becomes too long with many cards, switch to tabs | agent |
| D034 |  | architecture | Finance module route prefix | UI routes at /finance/*, API routes at /api/categories, /api/budgets, /api/transactions, /api/cashflow-payments | Finance — это distinct top-level domain наравне с CRM, Procurement, Projects. Используем установленный pattern: /{module}/* для UI, /api/{resource} для API. Resources множественные (categories не finance) следуя pattern из /api/contacts, /api/counterparties. | yes | agent |
| D035 |  | M006/S01 | Category hierarchy implementation | Flat list with parentId (adjacency list pattern), not nested sets or materialized path | Schema уже имеет parentId field. Adjacency list проще всего для CRUD queries. UI рендерит иерархию с отступами (recursion по parentId). Для глубокой иерархии >3 уровней можно будет перейти на nested sets, но для MVP достаточно adjacency list. | yes | agent |
| D036 |  | M006/S01 | Category soft-delete mechanism | Use isActive flag instead of deletedAt timestamp | Category schema имеет isActive: Boolean @default(true), но не имеет deletedAt. Pattern отличается от Transaction (deletedAt) и Counterparty (deletedAt). Для Categories used flag более семантичен — категории не удаляются, а деактивируются. | yes | agent |
| D037 |  | M006/S04 | CashFlowPayment status workflow | Status enum: planned → scheduled → paid, с возможностью cancelled из любого состояния | Платеж проходит lifecycle: запланирован (planned), назначен к оплате (scheduled), оплачен (paid). Cancelled позволяет отменить платеж из любого состояния. Это упрощённая версия state machine — без reopen, без возврата из paid. Следует pattern из M005 (Invoice statuses, Delivery statuses). | yes | agent |
